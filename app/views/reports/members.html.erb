<div class="panel-heading">
  Members Report
</div>

<div class="panel-body">
  <%= simple_form_for :search, url:  reports_members_path, remote: true, html: { novalidate: true } do |f| %>

      <div class="row">
        <div class="col-sm-6">
          <div id="report_members_organization_unit_tree"></div>
          <%=  f.input :organization_unit, as: :hidden, required: false %>
        </div>
        <div class="col-sm-6">
          <%=  f.input :membership_type, collection: MembershipType.all, required: false, prompt: true %>
          <fieldset class="sub_form">
            <legend>Payment Status</legend>
          <%=  f.input :budget_year, collection: BudgetYear.all, required: false, prompt: true %>
          <%=  f.input :payment_status, collection: [['Paid', "paid"], ['Not Paid', false]], required: false, prompt: true %>
          </fieldset>
        </div>
      </div>
      <div class="form-actions">
        <%= f.button :button, 'Reset Form', type: :reset, class: 'btn btn-default' %>
        <%= f.button :submit, 'Search', class: 'btn btn-primary' %>
      </div>
  <% end %>


  <div id="members_report">
    <%= render 'members' %>
  </div>
</div>