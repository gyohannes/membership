<%= simple_form_for(@profession_category) do |f| %>
  <%= f.error_notification %>
  <%= f.input :name, class: "form-control show-tick" %>

    <div class="form-actions">
    <%= f.button :submit, class: 'btn btn-primary' %>
  </div>
<% end %>
